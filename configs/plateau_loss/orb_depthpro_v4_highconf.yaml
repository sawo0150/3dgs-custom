# exp35: exp19 설정, high-confidence native anchor (obs>=10 & found_ratio>=0.5) (2026-07-09)
# exp19: MPS init + MPS cameras + DepthPro virtual anchors (v4, D_target=0.50m)
# Anchors generated using MPS keyframe poses → same world coordinate as MPS training
# Base: ellipsoidal plateau, same hyperparams as exp18 but trained on MPS full data

enabled: true
type: ellipsoidal

anchor_path: /home/wosas/Desktop/Incremental_mapping_test/gs_floaterLab/results/diagnostic/native_anchors_neworb_highconf_20260709_205327/anchors_all_depth_pro.npy
obs_min: 0

knn_k: 5
knn_iso_mult: 0

alpha_n: 0.4
alpha_t: 0.9
tau_min: 0.03
tau_n_max: 0.30
tau_t_max: 0.60

start_iter: 5000
lambda_plateau: 0.01

sample_size: 8192

pop2_zclip: true
pop2_z_threshold: 2.0
pop2_start_iter: 5000
pop2_interval: 1000
